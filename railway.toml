[build]
builder = "NIXPACKS"

[deploy]
startCommand        = "node --max-old-space-size=200 src/index.js"
healthcheckPath     = "/ping"
healthcheckTimeout  = 100
restartPolicyType   = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production.variables]
PORT = "3000"
